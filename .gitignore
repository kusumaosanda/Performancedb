# Secrets — never commit or push
.env

# Local deploy bookkeeping
.deploy_state.json

# macOS / Python noise
.DS_Store
__pycache__/
*.pyc

# Sample/reference data used while building pages — may contain employee names.
# The Performancedb repo is PUBLIC, so this must never be pushed.
samples/
